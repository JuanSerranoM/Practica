                <tbody>
                    {{#each usuario}}
                        
                     <tr class="">
                        <th scope="row">{{id_usuario}}</th>
                        <td> {{username}} </td>
                        <td> {{nombre}} </td>
                        <td> {{apellido}} </td>
                        <td> {{area}} </td>
                        <td> <input class="btn btn-sm btn-secondary text-light" type="button" value="Editar" onclick="editar({{id_usuario}})">
                        <input class="btn btn-sm btn-secondary text-light" type="button" value="Eliminar" onclick="eliminar({{id_usuario}})">
                        </td>

                        

                        </tr>
                    {{/each}}
                </tbody>